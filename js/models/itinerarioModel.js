export class ItinerarioModel {
    constructor(caminho, etapas) {
        this.caminho = caminho;
        this.etapas = etapas;
    }

    // Método para calcular o itinerário com base nas preferências do utilizador
    static construirItinerario(caminhos, partida, destino, dias) {
        // Lógica a implementar
        return null;
    }
} 