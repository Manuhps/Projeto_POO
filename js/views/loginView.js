// Função para exibir uma mensagem de alerta
function showAlert(message) {
    alert(message);
  }
  
  // Função para redirecionar o utilizador para outra página
  function redirectTo(url) {
    window.location.href = url;
  }
  
  export { showAlert, redirectTo };