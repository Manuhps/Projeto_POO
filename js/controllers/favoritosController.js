export class FavoritosController {
    constructor(view, favoritosModel) {
        this.view = view;
        this.favoritosModel = favoritosModel;
    }

    adicionarAosFavoritos(itinerario) {
        // Lógica para adicionar
    }

    removerDosFavoritos(id) {
        // Lógica para remover
    }

    mostrarFavoritos() {
        // Lógica para mostrar favoritos
    }
} 